#ifndef UTILS__H
#define UTILS__H
/*
typedef struct {
  int index;   //index in tuple
  char type;   //type of value
  char *name;  //name for error message
} paramCheck_t;

bool checkParams(PyObject *args, Py_ssize_t len, paramCheck_t *checks);
*/

#endif
